## 1. 本体与样例数据

- [x] 1.1 定义供应链 Demo 使用的运行时本体模型、对象类型、关系类型、函数、约束和动作类型清单
- [ ] 1.2 为 `Product`、`Warehouse`、`InventoryPosition`、`Shipment`、`PurchaseOrder`、`Supplier`、`DemandSignal`、`RiskAlert` 准备最小可演示字段集和解释元数据
- [ ] 1.3 准备模拟业务数据与案例集，覆盖三条缺货案例、两条非缺货案例和一条边界模糊案例
- [ ] 1.4 建立业务记录到本体实例与状态快照的映射规则，并验证缺货分析主案例可被正确装载

## 2. 后端语义与分析能力

- [ ] 2.1 实现本体只读查询接口，支持对象、动作、函数和关系的语义解释获取
- [x] 2.2 实现自然语言到 `BusinessIntent` 与 `OntologyContext` 的语义对齐流程
- [x] 2.3 实现缺货风险分析接口，输出自然语言结论、结构化证据链和派生指标结果
- [x] 2.4 为未知对象、非法动作和缺失时间窗等输入补充澄清与拒绝处理

## 3. 规划与重规划能力

- [ ] 3.1 定义 `PlanStep` 结构，并为每个合法 `ActionType` 建立前置条件、输入参数和预期效果约束
- [ ] 3.2 实现基于本体合法动作边界的计划生成接口，输出计划步骤、假设和 KPI 影响
- [ ] 3.3 实现追加限制条件后的重规划接口，输出步骤保留、替换、删除和新增的差异结果
- [ ] 3.4 为主案例验证“不能空运”等限制条件可以稳定触发可解释重规划

## 4. Web Demo 与验收

- [x] 4.1 实现三联屏 Web Demo，展示对话区、语义/证据区和计划区
- [x] 4.2 将问答分析、证据解释、计划生成和重规划流程串成单会话主链路
- [x] 4.3 支持在界面中切换预置案例，并查看本体解释与证据来源
- [x] 4.4 编写 Demo 验收脚本，验证主链路、非缺货案例、非法动作处理和重规划差异展示
