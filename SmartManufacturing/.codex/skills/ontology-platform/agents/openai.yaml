interface:
  display_name: "Ontology Platform"
  short_description: "本体驱动业务问答统一入口"
  default_prompt: "Use $ontology-platform to classify an ontology-grounded business question, inspect ontology and capability metadata, and route it into query, known-flow, gap-planning, or capability-authoring mode."

policy:
  allow_implicit_invocation: true
