schema_version: 1
id: baseline-direct-context
revision: dev
title: Direct Ontology Context Baseline
description: 在 Agent Prompt 中内嵌完整精简测试本体并直接生成数据查询任务。
mutable: true

runtime:
  command: opencode
  args:
    - acp
    - --print-logs
    - --pure
  cwd: ../..
  state_dir: ../../.runtime/opencode/baseline-direct-context
  startup_timeout_ms: 15000

opencode:
  config: opencode/opencode.jsonc
  assets:
    - opencode/prompt.md

model:
  id: deepseek/deepseek-v4-flash
  source: opencode
  auth:
    source: opencode

skills: []

ontology:
  id: smart-building-sample

environment:
  required: []
