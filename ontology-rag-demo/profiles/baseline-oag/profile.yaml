schema_version: 1
id: baseline-oag
revision: dev
title: OAG Retrieval Baseline
description: 使用 BGE-M3 Top 5 与最小连通子图上下文生成数据查询任务。
mutable: true

runtime:
  command: opencode
  args:
    - acp
    - --print-logs
    - --pure
  cwd: ../..
  state_dir: ../../.runtime/opencode/baseline-oag
  startup_timeout_ms: 15000

opencode:
  config: opencode/opencode.jsonc
  assets:
    - opencode/prompt.md

model:
  id: deepseek/deepseek-v4-flash
  source: opencode
  auth:
    source: opencode

skills:
  - id: ontology-retrieval
    path: skills/ontology-retrieval

retrieval:
  endpoint:
    env: OAG_BASE_URL
  vector_top_k: 5
  graph_algorithm: minimum_connected_subgraph

ontology:
  id: smart-building-sample

environment:
  required:
    - OAG_BASE_URL
